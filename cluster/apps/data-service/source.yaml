apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: data-service
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/doemefu/homelab-data-service
  ref:
    branch: main
  secretRef:
    name: data-service-flux-auth
