apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Applied file by file by infra/playbooks/41_monitoring.yml; this kustomization exists for
# CI (kubeconform + conftest) and for the `kubectl apply -k` / `kubectl delete -k` spike
# and rollback path in DEPLOYMENT.md "coroot-node-agent (NM-2)".
resources:
  - daemonset.yaml
  - service.yaml
  - servicemonitor.yaml
  - networkpolicy.yaml
