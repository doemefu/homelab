name: "Ansible Lint"
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ansible/ansible-lint@v26
        with:
          requirements_file: infra/requirements.yml
