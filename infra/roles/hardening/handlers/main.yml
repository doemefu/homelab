---
- name: Reload sshd
  ansible.builtin.service:
    name: ssh
    state: reloaded

- name: Restart fail2ban
  ansible.builtin.service:
    name: fail2ban
    state: restarted
  ignore_errors: "{{ ansible_check_mode }}"

- name: Reload ufw
  community.general.ufw:
    state: enabled
